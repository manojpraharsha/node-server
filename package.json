{
  "name": "myanatomy-website-backend",
  "version": "1.0.0",
  "description": "MyAnatomy Backend",
  "main": "app.js",
  "scripts": {
    "prestart:prod": "npm run clean && npm run build",
    "prod:start": "node build/index.js",
    "build": " --out-dir build/ --ignore node_modules/",
    "clean": "rm -rf build && mkdir build",
    "start:production": "NODE_ENV=production node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manoj Praharsha",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-handlebars": "^5.3.1",
    "mongoose": "^5.9.18",
    "nodemailer": "^6.4.8"
  }
}